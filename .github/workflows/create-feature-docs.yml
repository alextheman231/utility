name: Create feature docs

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  create-feature-docs:
    uses: alextheman231/github-actions/.github/workflows/create-feature-docs.yml@a00c59786bf2f81fe88d9084008fc05452dc45b2 # v9.0.2
    secrets:
      APP_ID: ${{ secrets.ALEX_UP_BOT_APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.ALEX_UP_BOT_PRIVATE_KEY }}
