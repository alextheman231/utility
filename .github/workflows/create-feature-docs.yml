name: Create feature docs

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  create-feature-docs:
    uses: alextheman231/github-actions/.github/workflows/create-feature-docs.yml@3db51f2a57ee95cda50310b3856cfdc0ae171cf7 # v8.3.3
    secrets:
      APP_ID: ${{ secrets.ALEX_UP_BOT_APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.ALEX_UP_BOT_PRIVATE_KEY }}
