[**@alextheman/utility v4.12.3**](../README.md)

***

[@alextheman/utility](../globals.md) / parseBoolean

# Function: parseBoolean()

> **parseBoolean**(`inputString`): `boolean`

Takes a stringly-typed boolean and converts it to an actual boolean type.

## Parameters

### inputString

`string`

The string to parse.

## Returns

`boolean`

The string parsed as an actual boolean.

## Throws

If the string is not either `true` or `false` (case insensitive).
