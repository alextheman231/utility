[**@alextheman/utility v4.14.0**](../README.md)

***

[@alextheman/utility](../globals.md) / isTemplateStringsArray

# Function: isTemplateStringsArray()

> **isTemplateStringsArray**(`input`): `input is TemplateStringsArray`

Determines whether or not the input is a valid `TemplateStringsArray`.

## Parameters

### input

`unknown`

The input to check

## Returns

`input is TemplateStringsArray`

`true` if the input is a valid `TemplateStringsArray`, and false otherwise. The type of the input will also be narrowed down to `TemplateStringsArray` if `true`.
