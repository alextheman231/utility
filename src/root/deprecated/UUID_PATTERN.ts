import { UUID_REGEX_PATTERN } from "src/root/constants/UUID_REGEX";

/** @deprecated This has been renamed to `UUID_REGEX_PATTERN`. */
const UUID_PATTERN = UUID_REGEX_PATTERN;

export default UUID_PATTERN;
